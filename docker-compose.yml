version: '3'
services:
  gcloud:
    image: google/cloud-sdk
    volumes:
      - ~/.config/gcloud:/root/.config/gcloud
      - ~/.ssh:/root/.ssh
    environment:
      - CLOUDSDK_CONFIG=/root/.config/gcloud
      - TERM=xterm-256color
      - USER
